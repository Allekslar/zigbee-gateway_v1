# [RPT-005] Інтегрувати `ReportingManager` в `ServiceRuntime::apply_managers/tick`, додати runtime counters (`reporting_retries`, `reporting_failures`, `stale_devices`)

## Summary
- **Status (from backlog):** `TODO`
- **Component:** `service`
- **Source:** `arh/Reporting_Backlog.md`

## Description
Інтегрувати `ReportingManager` в `ServiceRuntime::apply_managers/tick`, додати runtime counters (`reporting_retries`, `reporting_failures`, `stale_devices`)

## Scope
- **Files (edit/create):** `components/service/include/service_runtime.hpp`, `components/service/service_runtime.cpp`

## Dependencies
- RPT-004

## Definition of Done
- [ ] Single-writer збережено; нові лічильники доступні через `stats()`
- [ ] Architecture guardrails respected (Core domain-only events, thin HAL, single-writer ingress, bounded memory).
- [ ] Docs/contracts updated if API or architecture boundary changed.

## Test Plan
- [ ] `test/host/test_service_runtime.cpp`
- [ ] `bash ./check_arch_invariants.sh`
- [ ] Relevant host/target tests pass in CI

## Notes
- Keep PR scope focused on this issue only (no mixed refactors).
