# [RPT-021] Оновити `GET /api/devices` JSON (`reporting_state`, `last_report_at`, `stale`, `temperature_c`, `occupancy`, `contact`, `battery`, `lqi`, `rssi`)

## Summary
- **Status (from backlog):** `TODO`
- **Component:** `web_ui`
- **Source:** `arh/Reporting_Backlog.md`

## Description
Оновити `GET /api/devices` JSON (`reporting_state`, `last_report_at`, `stale`, `temperature_c`, `occupancy`, `contact`, `battery`, `lqi`, `rssi`)

## Scope
- **Files (edit/create):** `components/web_ui/web_handlers_device.cpp`

## Dependencies
- RPT-020

## Definition of Done
- [ ] API віддає консистентний snapshot без часткових полів
- [ ] Architecture guardrails respected (Core domain-only events, thin HAL, single-writer ingress, bounded memory).
- [ ] Docs/contracts updated if API or architecture boundary changed.

## Test Plan
- [ ] `test/host/test_web_handlers_network_routes.cpp`, `test/integration/test_integration_web_handlers_device.cpp`
- [ ] `bash ./check_arch_invariants.sh`
- [ ] Relevant host/target tests pass in CI

## Notes
- Keep PR scope focused on this issue only (no mixed refactors).
